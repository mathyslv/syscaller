[package]
name = "syscaller"
version = "0.1.0"
edition = "2024"

[features]
default = []
macro = ["syscaller-wrap-macro"]

[dependencies]
syscaller-wrap-macro = { path = "../syscaller-wrap-macro", optional = true }
